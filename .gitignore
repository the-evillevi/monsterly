node_modules/
.pnpm-store/
dist/
.env
.env.*
!.env.example
.DS_Store
.claude/

# Real member data (names + phone numbers) stays local; only the fake example
# is committed.
scripts/data/*.json
!scripts/data/membresias.example.json
# Transient data migrations generated from the real data are never committed.
supabase/migrations/*_transient_import_*.sql
